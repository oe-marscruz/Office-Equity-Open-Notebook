'use strict';

// Preload script for the Open Notebook desktop window.
// The app UI is a plain web app served by the bundled Next.js frontend, so we
// keep the preload minimal. contextIsolation is enabled; expose nothing here
// unless a future feature needs IPC.
